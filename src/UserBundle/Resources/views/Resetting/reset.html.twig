{% extends "@FOSUser/layout.html.twig" %}

{% block fos_user_content %}
    {% include "@FOSUser/Resetting/reset_content.html.twig" %}
{#
    <form action="{{ path("fos_user_resetting_reset") }}" method="post">
        {% if csrf_token %}
            <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
        {% endif %}
        <div class="form-login-box">
            <div id="first" data-role="none">
                <label for="first"><h5>Username ou Addresse Email</h5></label>
                <input type="text" id="first" name="_username" placeholder="Entrer votre nom d'utilisateur"  value="{{ last_username }}" required="required" autocomplete="username" />
            </div>

            <div class="btn-submut">
                <button type="submit" data-role="none">Reset password</button>
            </div>
        </div>
    </form>
    #}
{% endblock fos_user_content %}
